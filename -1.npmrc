# .npmrc
# Tell npm to skip optional packages that don't match the current OS/CPU.
# This fixes the @next/swc-win32-x64-msvc error when deploying to Vercel (Linux)
# from a machine where package-lock.json was generated on Windows.
optional=true
